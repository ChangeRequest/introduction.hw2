package school.lemon.changerequest.java.introduction.hw2;

import java.util.Objects;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Task9 {
    public static String[] extractData(String URL) {
        if (URL == null) {
            return new String[]{"", "", ""};
        }
//        Pattern port = Pattern.compile(".*:(\\d+).*");
//        Matcher portm = port.matcher(url);
//        Pattern scheme = Pattern.compile(":\\\\([\\w\\-.]+)|(www\\.\\w+.\\w+)\n");
//        Matcher schemem = scheme.matcher(url);
//        Pattern host = Pattern.compile("([a-zA-Z]+)://");
//        Matcher hostm = host.matcher(url);

        return new String[]{"", "", ""};
    }
}